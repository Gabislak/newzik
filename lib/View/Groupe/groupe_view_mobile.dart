import 'package:flutter/material.dart';

class GroupeViewMobile extends StatelessWidget {
  const GroupeViewMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Groupe View'),
    );
  }
}
