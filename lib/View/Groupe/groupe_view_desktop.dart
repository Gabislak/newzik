import 'package:flutter/material.dart';

class GroupeViewDesktop extends StatelessWidget {
  const GroupeViewDesktop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Groupe View'),
    );
  }
}
