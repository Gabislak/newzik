import 'package:flutter/material.dart';

class GroupeView extends StatelessWidget {
  const GroupeView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Groupe View'),
    );
  }
}
