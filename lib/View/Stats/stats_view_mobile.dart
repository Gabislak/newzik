import 'package:flutter/material.dart';

class StatsViewMobile extends StatelessWidget {
  const StatsViewMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Statistiques View'),
    );
  }
}
