import 'package:flutter/material.dart';

class AlbumSemaineView extends StatelessWidget {
  const AlbumSemaineView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Album de la semaine View'),
    );
  }
}
